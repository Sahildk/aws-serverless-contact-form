import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Contact Us | AWS Serverless Contact Form",
  description:
    "Get in touch with us. Powered by a fully serverless AWS architecture — Next.js, AWS Lambda, API Gateway, and Amazon SES.",
  keywords: ["contact", "serverless", "AWS", "Next.js", "Lambda"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} dark h-full antialiased`}>
      <body className="min-h-full flex flex-col bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
